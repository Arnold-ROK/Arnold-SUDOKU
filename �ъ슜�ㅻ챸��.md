# RYK SUDOKU 설치 안내

이 폴더에는 다음 7개 파일이 들어 있습니다. **하나도 빠뜨리지 말고, 폴더 구조를 바꾸지 말고 그대로** 올려야 합니다.

```
index.html                 ← 게임 본체
manifest.webmanifest       ← 앱 이름·아이콘 정보
sw.js                      ← 오프라인 실행 담당
icon-180.png               ← 아이폰 홈 화면 아이콘
icon-192.png
icon-512.png
icon-maskable-512.png      ← 안드로이드 원형 아이콘
```

---

## 1부. 배포하는 사람 (한 번만 하면 됩니다)

### 준비물
- GitHub 계정 (무료). 없으면 github.com에서 가입하세요.

### 단계

**① 저장소 만들기**
1. github.com 로그인 → 오른쪽 위 `+` → **New repository**
2. Repository name: `ryk-sudoku` (소문자·영문 권장)
3. **Public** 선택 (Private이면 페이지가 안 열립니다)
4. **Create repository** 클릭

**② 파일 올리기**
1. 만들어진 저장소 화면에서 **uploading an existing file** 링크 클릭
   (또는 Add file → Upload files)
2. 위의 7개 파일을 **전부 선택해서** 끌어다 놓기
   ※ 폴더째 올리지 말고 파일 7개를 직접 올리세요. `index.html`이 최상단에 있어야 합니다.
3. 아래 **Commit changes** 클릭

**③ 웹에 공개하기 (GitHub Pages 켜기)**
1. 저장소 상단 **Settings** → 왼쪽 메뉴 **Pages**
2. Source: **Deploy from a branch**
3. Branch: **main**, 폴더: **/ (root)** → **Save**
4. 1~3분 기다린 뒤 그 화면을 새로고침하면 주소가 나타납니다

**④ 주소 확인**
```
https://<본인 GitHub 아이디>.github.io/ryk-sudoku/
```
이 주소를 카톡·문자·메일로 보내면 끝입니다.

### 나중에 게임을 수정하고 싶으면
새 `index.html`을 같은 저장소에 다시 올리면(같은 이름으로 덮어쓰기) 1~2분 뒤 반영됩니다.
단, 이미 설치한 사람은 캐시 때문에 예전 화면이 보일 수 있습니다. 이때는 `sw.js` 파일 첫 줄의
`ryk-sudoku-v1`을 `ryk-sudoku-v2`로 바꿔서 함께 올리면 모두에게 새 버전이 내려갑니다.

---

## 2부. 받는 사람 (링크만 있으면 됩니다)

### 아이폰 / 아이패드
1. **Safari로** 링크를 엽니다 (다른 브라우저는 설치가 안 될 수 있습니다)
2. 화면 아래 **공유 버튼**(□에 ↑ 표시) 누르기
3. 목록을 내려서 **홈 화면에 추가** 누르기
4. 오른쪽 위 **추가** → 홈 화면에 RYK SUDOKU 아이콘 생성

### 안드로이드
1. **Chrome으로** 링크를 엽니다
2. 오른쪽 위 **⋮** 메뉴 → **앱 설치** 또는 **홈 화면에 추가**
3. **설치** 누르기

### PC (Windows / Mac)
1. Chrome 또는 Edge로 링크를 엽니다
2. 주소창 오른쪽 끝의 **설치 아이콘**(⊕ 또는 모니터 모양) 클릭 → **설치**

설치하지 않고 브라우저에서 링크만 열어서 써도 게임은 똑같이 됩니다.

---

## 3부. 알아두면 좋은 점

- **오프라인 실행**: 한 번 열어두면 그다음부터는 인터넷 없이도 실행됩니다 (비행기 안, 지하철에서도 가능).
- **기록 저장**: 푼 횟수와 최단·최장 시간은 **각자 본인 기기에만** 저장됩니다. 다른 사람 기록과 섞이지 않고, 서로 볼 수도 없습니다.
- **풀던 판 이어하기**: 앱을 닫아도 진행 중이던 판과 시간이 저장되어, 다시 열면 이어서 풀 수 있습니다.
- **기록이 사라지는 경우**: 앱(아이콘) 삭제, 브라우저 인터넷 사용 기록·사이트 데이터 삭제, 시크릿 모드 사용. 기기를 바꾸면 기록은 옮겨지지 않습니다.
- **아이폰 주의**: Safari에서 설치한 앱은 오랫동안(수 주 이상) 한 번도 열지 않으면 iOS가 저장 데이터를 정리할 수 있습니다.

---

## 4부. 게임 조작법

- **난이도 버튼**: 누르면 그 난이도의 새 퍼즐이 생성됩니다 (답이 하나뿐인 퍼즐만 출제).
- **멈춤**: 시간이 멈추고 판이 가려집니다. *이어서 풀기*를 누르면 멈춘 시각부터 계속됩니다.
- **메모(✎)**: 켜고 숫자를 누르면 칸 안에 작은 후보 숫자로 적힙니다. 같은 숫자를 다시 누르면 지워집니다. 확정할 때는 메모를 끄고 숫자를 누르세요.
- **힌트(💡)**: 빈칸을 먼저 고른 뒤 누르면 그 칸의 정답이 보라색으로 채워지고 잠깁니다. 힌트를 쓴 판은 푼 횟수에는 들어가지만 최단·최장 시간 기록에는 반영되지 않습니다.
- **되돌리기(↺) / 지우기(⌫)**: 직전 입력 취소 / 선택한 칸 비우기.
- **녹색 표시**: 가로줄·세로줄·3×3 박스가 1~9로 정확히 채워지면 그 아홉 칸이 녹색으로 바뀝니다.
- **PC 단축키**: 숫자키 1~9 입력, Backspace 지우기, 방향키 이동, H 힌트, N 메모.
